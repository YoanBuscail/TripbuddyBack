{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h2 class="mb-4 text-center">Dashboard Administrateur</h2>
        
        <div class="row">
            <div class="col-sm-6 mb-3 mb-sm-0">
                <div class="card text-center">
                    <div class="card-body">
                        <h5 class="card-title">Gérer les utilisateurs</h5>
                        <p class="card-text"><i class="bi bi-people-fill mr-3"></i></p>
                        <a href="{{ path('app_back_user_list') }}" class="btn btn-primary">Accéder</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-6">
                <div class="card text-center">
                    <div class="card-body">
                        <h5 class="card-title">Gérer les itinéraires</h5>
                        <p class="card-text"><i class="bi bi-geo-alt-fill mr-3"></i></p>
                        <a href="{{ path('app_back_itinerary_list') }}" class="btn btn-success">Accéder</a>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}

 